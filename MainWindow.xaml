<Window x:Class="DigitizePlot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DigitizePlot"
        mc:Ignorable="d"
        xmlns:ribbon="http://schemas.microsoft.com/winfx/2006/xaml/presentation/ribbon"
        Title="DigitizePlot" Height="800" Width="1200" Loaded="Window_Loaded" PreviewKeyDown="Window_KeyDown" Initialized="Window_Initialized" Closing="Window_Closing">
    <DockPanel>
        <Ribbon DockPanel.Dock="Top" Loaded="Ribbon_Loaded">
            <RibbonTab>
                <!-- File group-->
                <RibbonGroup x:Name="FileGroup" Header="File">
                    <RibbonButton Label="Open image" SmallImageSource="/Images/OpenImage.png" Click="OpenImage_Click" ToolTipTitle="Open and load a new image file"/>
                    <RibbonButton Label="Save data" SmallImageSource="/Images/Save.png" Click="Save_Click" ToolTipTitle="Save current point data"/>
                    <RibbonButton Label="Load data" SmallImageSource="/Images/Open.png" Click="Open_Click" ToolTipTitle="Restore previously saved point data"/>
                </RibbonGroup>
                <!-- Clipboard group-->
                <RibbonGroup x:Name="ClipboardGroup" Header="Clipboard">
                    <RibbonButton Label="Load image" SmallImageSource="/Images/ClipboardIn.png" Click="ClipboardIn_Click" ToolTipTitle="Paste image from clipboard"/>
                    <RibbonButton Label="Export data" SmallImageSource="/Images/ClipboardOut.png" Click="ClipboardOut_Click" ToolTipTitle="Export digitised data to clipboard"/>
                </RibbonGroup>
                <!-- Data group-->
                <RibbonGroup x:Name="DataGroup" Header="Data">
                    <RibbonButton Label="Clear all data" SmallImageSource="/Images/New.png" Click="Reset_Click" ToolTipTitle="Delete all data points, including axis"/>
                    <RibbonButton Label="Clear points" SmallImageSource="/Images/Clear.png" Click="Points_Click" ToolTipTitle="Delete all data points, excluding axis"/>
                    <RibbonCheckBox Name="cbSort" Label="Sort" SmallImageSource="/Images/Sort.png" IsChecked="{Binding SortMode, Mode=TwoWay}" ToolTipTitle="Sort data by X value" Click="cbSort_Clicked"/>
                    <RibbonComboBox Label="Data type X" SelectionBoxWidth="70" ToolTipTitle="X axis scaling">
                        <RibbonGallery Name="rcbX" SelectedValue="{Binding StyleX, Mode=TwoWay}" SelectionChanged="Type_SelectionChanged">
                            <RibbonGalleryCategory Name="rcbTypeX" />
                        </RibbonGallery>
                    </RibbonComboBox>
                    <RibbonComboBox Label="Data type Y" SelectionBoxWidth="70" ToolTipTitle="Y axis scaling">
                        <RibbonGallery Name="rcbY" SelectedValue="{Binding StyleY, Mode=TwoWay}" SelectionChanged="Type_SelectionChanged">
                            <RibbonGalleryCategory Name="rcbTypeY"/>
                        </RibbonGallery>
                    </RibbonComboBox>
                </RibbonGroup>
                <!-- View group-->
                <RibbonGroup x:Name="ViewGroup" Header="View">
                    <RibbonTextBox Name="tbOpacity" Label="Opacity" Text="{Binding MainOpacity, StringFormat=\{0:N2\}, Mode=TwoWay}" TextBoxWidth="50" LostKeyboardFocus="TextBox_LostFocus" ToolTipTitle="Set opacity of main view image"/>
                    <RibbonTextBox Name="tbMagnify" Label="Magnify" Text="{Binding ZoomScale, StringFormat=\{0:N1\}, Mode=TwoWay}" TextBoxWidth="50" LostKeyboardFocus="TextBox_LostFocus" ToolTipTitle="Magnification for details view"/>
                    <RibbonCheckBox Name="cbGuides" Label="Axis guides" SmallImageSource="/Images/Guide.png" IsChecked="{Binding AxisGuides, Mode=TwoWay}" ToolTipTitle="Horizontal and vertical axis point guides" Click="cbGuides_Click"/>
                    <RibbonCheckBox Name="cbNearestNeighbour" Label="Nearest neighbour" SmallImageSource="/Images/Bitmap.png" IsChecked="{Binding NearestNeighbour, Mode=TwoWay}" ToolTipTitle="Use nearest neighbour bitmap interpolation" Click="cbNearestNeighbour_Click"/>
                </RibbonGroup>
                <!-- Auto group-->
                <RibbonGroup x:Name="AutoGroup" Header="Auto mode">
                    <RibbonCheckBox Name="cbAuto" Label="Auto on" LargeImageSource="/Images/Wizard.png" IsChecked="{Binding AutoMode, Mode=TwoWay}" ToolTipTitle="Auto data detection"/>
                    <RibbonTextBox Name="tbTolerance" Label="Tolerance" Text="{Binding AutoTolerance, StringFormat=\{0:N0\}, Mode=TwoWay}" TextBoxWidth="50" ToolTipTitle="Auto detection theshold (rms R,G,B)" LostKeyboardFocus="TextBox_LostFocus"/>
                    <RibbonTextBox Name="tbPixelStep" Label="Pixel step" Text="{Binding PixelStep, StringFormat=\{0:N0\}, Mode=TwoWay}" TextBoxWidth="50" ToolTipTitle="Pixels earch distance" LostKeyboardFocus="TextBox_LostFocus"/>
                    <RibbonTextBox Name="tbSpacing" Label="Spacing" Text="{Binding AutoSpacing, StringFormat=\{0:N0\}, Mode=TwoWay}" TextBoxWidth="50" ToolTipTitle="Auto point spacing (screen pixels)" LostKeyboardFocus="TextBox_LostFocus"/>
                    <RibbonComboBox Label="Spacing type" SelectionBoxWidth="70" ToolTipTitle="Method used for point spacing">
                        <RibbonGallery Name="rcbSpacing" SelectedValue="{Binding AutoSpaceType}" SelectionChanged="Type_SelectionChanged">
                            <RibbonGalleryCategory Name="rcbTypeSpacing" />
                        </RibbonGallery>
                    </RibbonComboBox>

                </RibbonGroup>
                <!-- Auto group-->
                <RibbonGroup x:Name="HelpGroup" Header="Help">
                    <RibbonButton Label="Help" LargeImageSource="/Images/Help.png" Click="Help_Click" ToolTipTitle="Help description of tool with examples"/>
                </RibbonGroup>
            </RibbonTab>
        </Ribbon>
        <StatusBar x:Name="statusBar" Height="30" VerticalAlignment="Bottom" DockPanel.Dock="Bottom" VerticalContentAlignment="Bottom" Margin="0" HorizontalContentAlignment="Stretch">
            <StatusBarItem x:Name="statusImageSize" HorizontalAlignment="Left"/>
            <StatusBarItem x:Name="statusViewSize" HorizontalAlignment="Left"/>
            <StatusBarItem x:Name="statusVersion" Content="{Binding Version, Mode=OneWay}" ContentStringFormat="Version {0}" HorizontalAlignment="Right"/>
        </StatusBar>
        <Grid d:DataContext="{d:DesignInstance local:MainWindow}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Grid.Column="2"/>
            <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" Grid.RowSpan="3" HorizontalAlignment="Stretch"/>
            <Canvas Name="MainCanvas" Grid.Column="0" Grid.Row="0" Margin="0" Grid.RowSpan="3"/>
            <Image Name="MainImage" Grid.Row="0" Grid.Column="0"  Margin="0" AllowDrop="True" Drop="MainImage_Drop" Source="/Plot.png" Stretch="Fill"
               MouseMove="MainImage_MouseMove" Cursor="Cross" MouseDown="MainImage_MouseDown" MouseUp="MainImage_MouseUp"
               Opacity="{Binding MainOpacity, Mode=TwoWay}" Grid.RowSpan="3" SizeChanged="MainImage_SizeChanged"/>
            <Image Name="SubImage" Grid.Row="0" Grid.Column="2" Margin="0" Stretch="Fill" Source="/Plot.png" SizeChanged="SubImage_SizeChanged" />
            <Canvas Name="SubCanvas" Grid.Row="0" Grid.Column="2" Margin="0">
                <Line Name="HLine" X1="0" Y1="0" X2="0" Y2="0" Stroke="{Binding LineColor}" StrokeThickness="1" />
                <Line Name="VLine" X1="0" Y1="0" X2="0" Y2="0" Stroke="{Binding LineColor}" StrokeThickness="1" />
            </Canvas>
            <Grid Grid.Column="2" Grid.Row="2">
                <Grid Margin="4">
                    <Label VerticalAlignment="Top" Margin="0,0,104,0" HorizontalAlignment="Right" Width="86" Content="Origin X" VerticalContentAlignment="Top" HorizontalContentAlignment="Right" Padding="0,0,5,0"/>
                    <Label VerticalAlignment="Top" Margin="0,20,104,0" HorizontalAlignment="Right" Width="86" Content="Origin Y" VerticalContentAlignment="Top" HorizontalContentAlignment="Right" Padding="0,0,5,0"/>
                    <Label VerticalAlignment="Top" Margin="0,40,104,0" HorizontalAlignment="Right" Width="86" Content="X offset" VerticalContentAlignment="Top" HorizontalContentAlignment="Right" Padding="0,0,5,0"/>
                    <Label VerticalAlignment="Top" Margin="0,60,104,0" HorizontalAlignment="Right" Width="86" Content="Y offset" VerticalContentAlignment="Top" HorizontalContentAlignment="Right" Padding="0,0,5,0"/>
                    <TextBox Name="OriginX" Text="{Binding data[0].X, StringFormat=\{0:G6\}, Mode=OneWayToSource}" LostKeyboardFocus="TextBox_LostFocus" 
                         VerticalAlignment="Top" Margin="0,0,0,0" HorizontalAlignment="Right" Width="100"/>
                    <TextBox Name="OriginY" Text="{Binding data[0].Y, StringFormat=\{0:6\}, Mode=OneWayToSource}" LostKeyboardFocus="TextBox_LostFocus" 
                         VerticalAlignment="Top" Margin="0,20,0,0" HorizontalAlignment="Right" Width="100"/>
                    <TextBox Name="XAxis" Text="{Binding data[1].X, StringFormat=\{0:G6\}, Mode=OneWayToSource}" LostKeyboardFocus="TextBox_LostFocus" 
                         VerticalAlignment="Top" Margin="0,40,0,0" HorizontalAlignment="Right" Width="100"/>
                    <TextBox Name="YAxis" Text="{Binding data[2].Y, StringFormat=\{0:G6\}, Mode=OneWayToSource}" LostKeyboardFocus="TextBox_LostFocus" 
                         VerticalAlignment="Top" Margin="0,60,0,0" HorizontalAlignment="Right" Width="100"/>
                    <Rectangle x:Name="Swatch" HorizontalAlignment="Left" Height="30" Margin="5,5,0,0" Stroke="Black" VerticalAlignment="Top" Width="30"/>
                    <TextBlock Name ="tbCoordX" HorizontalAlignment="Left" Margin="5,40,0,0" TextWrapping="Wrap" Text="X" VerticalAlignment="Top"/>
                    <TextBlock Name ="tbCoordY" HorizontalAlignment="Left" Margin="5,60,0,0" TextWrapping="Wrap" Text="Y" VerticalAlignment="Top"/>
                </Grid>
                <DataGrid Name="ResultsGrid" HeadersVisibility="Column" 
                  AutoGenerateColumns="False" CanUserReorderColumns="False" CanUserSortColumns="False" CanUserAddRows="False" Margin="0,88,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Label" Binding="{Binding Label, Mode=OneWay}" Width="Auto" IsReadOnly="True" Visibility="Collapsed"/>
                        <DataGridTextColumn Header="X" Binding="{Binding X, StringFormat=\{0:G6\}, Mode=TwoWay}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Y" Binding="{Binding Y, StringFormat=\{0:G6\}, Mode=TwoWay}" Width="*" IsReadOnly="True"/>
                    </DataGrid.Columns>
                    <DataGrid.RowStyle>
                        <Style TargetType="{x:Type DataGridRow}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
